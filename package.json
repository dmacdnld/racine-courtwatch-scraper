{
  "name": "racine-courtwatch-scraper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@azure/identity": "^4.8.0",
    "@azure/keyvault-secrets": "^4.9.0",
    "@prisma/client": "^6.0.1",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "prisma": "^6.0.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
